# Trading Club – Landing + Design System (React + Vite + Tailwind + Framer Motion + Lenis)

## Requirements
- Node.js 18+ (recommended)

## Run locally
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
npm run preview
```

## Notes
- Uses:
  - React
  - TypeScript
  - Framer Motion (animations)
  - Lenis (smooth scrolling) via `lenis` npm package
  - Google Fonts (Inter)
- HSTS + HTTP/3 are server-level; an optional Netlify config is included in `/deploy`.
